language: python

python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - pypy
  - pypy3

install:
  - pip install PyYAML
  - pip install coveralls

script:
  - coverage run --branch --source configuration -m py.test

after_success:
  - coveralls
