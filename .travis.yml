language: python

python:
  - 2.7
  - 3.2
  - 3.3
  - 3.4
  - pypy

install:
  - pip install PyYAML
  - pip install coveralls

script:
  - coverage run --branch --source configuration -m py.test

after_success:
  - coveralls
